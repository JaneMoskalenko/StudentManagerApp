<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.admin.studentmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".app.GettingStartedActivity"/>
        <activity android:name=".app.Login.SignUpActivity"/>
        <activity android:name=".app.GroupListActivity"/>
        <activity android:name=".app.Student.StudentListActivity"/>
        <activity android:name=".app.GroupProfileActivity"/>
        <activity android:name=".app.Student.StudentProfileActivity"/>
        <activity android:name=".app.Lesson.LessonListActivity"/>
        <activity android:name=".app.Lesson.AddLessonActivity"/>
    </application>

</manifest>